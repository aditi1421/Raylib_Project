{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Mac",
            "command": "make build_mac",
            "options": { "cwd": "${workspaceFolder}" }
        }
    ],
    "version": "2.0.0"
}